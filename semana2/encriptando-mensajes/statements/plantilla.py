# Cómo obtines n?
n = 

# Un ciclo for al rededor de qué?
for :
  
  # como sabes de que persona es el mensaje?
	persona = 
  # como obtienes el mensaje=
	frase =
  
  # como inicializas el nuevo mensaje?
	nueva_frase = 
  
  # como sabes si el mensaje es de lorena?
	if persona == :
    # como recorres caracter a caracter un string?
		for c:
      #ya sabes que tienes que cambiar las letras, cómo cambias las letras a partir del numero que escogió lorena?
      # que ocurre con las ultimas letras cuando llegas cerca de la z?
			if  97 <= ord(c) <= 122:
				
			#cómo agragas los espacios?
      else:
				nueva_frase +=
		print(nueva_frase)
  
  # como sabes si el mensaje es de limon?
	elif persona == :
		# Lo mismo que haces para lorena pero aplicado a el recorrido de limon
		print(nueva_frase)